#include <iostream>
#include <vector>
#include <string>
#include <stdlib.h>
#include<iomanip>

int main ()

{
    // we use setbase for the base value

// std::cout << std::setbase(8) << 65 << std::endl;
// we will get the qutiont and the reminder

//for hexa decimal format
// std::cout << std::setbase(16) << 65;
// to convert the hexa decimal that is viceversa
// std::cout << std::setbase(10) << 0X41;

}