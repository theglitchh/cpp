#include <iostream>
using namespace std;

class Solution {
public:
    int secondlargest(int arr[], int &n) {

    }
};

int main() {
    Solution s;
    int arr[]={1,3,4,5};
    int n = sizeof(arr) / sizeof(arr[0]);
    s.secondlargest(arr,n);

   return 0;
}